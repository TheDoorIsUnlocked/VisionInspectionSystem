# SOP-AI 作业监控系统开发计划

> **目标**: 实现视频演示中的 SOP 实时检测功能  
> **参考视频**: `SOPAI智能视觉跟踪监控系统是一种基于人工智能和机器视觉技术的工业生产流程管理工具，旨在通过自动化手段对标准作业流程（SOP）的执行情况进行实时监控分析与优化.mp4`  
> **创建日期**: 2026-05-15  
> **开发框架**: .NET 8 + WPF + YoloDotNet

---

## 一、视频功能分析

### 1.1 视频展示的核心功能

| 功能模块 | 视频表现 | 技术需求 |
|---------|---------|---------|
| **主视频区域** | 640x480 实时画面，带检测框和骨架线 | 相机接入 + YOLO检测 + 姿态估计 |
| **手部姿态估计** | 绿色骨架线跟踪手指关节 | MediaPipe 或类似库 |
| **目标检测** | 红色框 + 中文标签 + 置信度(0.85-0.98) | YOLO 模型 |
| **SOP步骤清单** | 8步表格，当前步蓝色高亮，完成显示OK | 状态机 + UI绑定 |
| **耗时计时器** | 右上角显示经过时间 | 计时器组件 |
| **结果判定** | PASS/FAIL 绿色/红色大字体 | 状态判定逻辑 |
| **时间轴缩略图** | 底部显示每步操作快照 | 图像截取 + 缩略图列表 |

### 1.2 SOP流程（8步装配）

```
步骤1: 装说明书 → 步骤2: 装手机壳 → 步骤3: 封口 → 步骤4: 装密实袋
    ↓
步骤5: 放置手机 → 步骤6: 放置充电器 → 步骤7: 放置说明书 → 步骤8: 合盖
    ↓
结果: PASS
```

---

## 二、当前系统评估

### 2.1 已实现 ✅

- [x] 实时相机接入（海康相机）
- [x] YOLO目标检测（YoloDotNet）
- [x] SOP状态机框架
- [x] YAML配置驱动
- [x] 基础UI界面（WPF）
- [x] 实时检测循环（每帧处理）

### 2.2 缺失/有问题 ❌

| 问题 | 严重程度 | 文件位置 |
|------|---------|---------|
| 区域定义硬编码 | 🔴 严重 | `StepConditionEvaluator.cs:200` |
| 对象跟踪算法错误 | 🔴 严重 | `SOPStateMachine.cs:115` |
| 缺少手部姿态估计 | 🟡 中 | 需集成 MediaPipe |
| 无时间轴缩略图 | 🟡 中 | UI组件缺失 |
| 无耗时计时器 | 🟢 低 | 简单添加 |
| 步骤卡片样式简陋 | 🟢 低 | UI优化 |

---

## 三、开发阶段规划

### 阶段1：修复基础功能（必须先做）

**目标**: 让系统能正确检测SOP步骤，消除致命bug

#### 任务1.1: 修复区域定义硬编码问题
- **问题**: `GetZoneDefinition()` 返回固定坐标 (0.2, 0.2, 0.3, 0.3)
- **影响**: `object_in_zone` 条件类型无效
- **修复方案**: 
  - 修改 `StepConditionEvaluator` 构造函数，注入区域列表
  - 从 YAML 配置读取区域定义
  - 使用字典存储区域，支持按ID查找

#### 任务1.2: 修复对象跟踪IOU算法
- **问题**: 使用 `BoundingBox.GetHashCode()` 作为跟踪key，每帧都是新对象
- **影响**: 
  - `FrameCount` 永远为1
  - `IsStable()` 永远返回false
  - 内存泄漏
- **修复方案**:
  - 改用 IOU（交并比）匹配算法
  - IOU阈值设为0.3
  - 同类对象匹配，更新位置而非新建

#### 任务1.3: 完善PASS/FAIL结果展示
- **当前**: 只有基础状态显示
- **目标**: 
  - 添加明显的PASS/FAIL大字体显示
  - 绿色/红色颜色区分
  - 显示违规详情

**阶段1验收标准**:
- [ ] YAML配置的区域坐标能被正确读取
- [ ] 对象跟踪稳定，同一物体多帧识别为同一ID
- [ ] SOP完成时正确显示PASS/FAIL

---

### 阶段2：UI优化（让界面像视频）

**目标**: 优化界面视觉效果，接近视频演示

#### 任务2.1: 添加耗时计时器
- 在状态栏添加计时器显示
- 格式: `耗时: 00:20`
- 从第一步开始计时，到PASS/FAIL结束

#### 任务2.2: 优化步骤卡片样式
- 当前步骤: 蓝色高亮背景 (#00bfff)
- 已完成步骤: 浅蓝色背景 + 绿色OK标记
- 未开始步骤: 灰色背景
- 字体: 14px 粗体

#### 任务2.3: 添加时间轴缩略图组件
- 底部横向滚动区域
- 每步完成后截取当前帧缩略图
- 显示步骤编号（蓝色圆圈）
- 最多显示4个，新步骤加入时左移

**阶段2验收标准**:
- [ ] 计时器准确显示经过时间
- [ ] 步骤卡片样式与视频一致
- [ ] 时间轴显示每步缩略图

---

### 阶段3：高级功能（可选）

**目标**: 添加手部姿态估计（视频中的绿色骨架线）

#### 任务3.1: 集成MediaPipe手部检测
- 评估 MediaPipe.NET 或 OpenPose
- 集成到检测流程
- 21点手部关键点检测

#### 任务3.2: 实时骨架叠加显示
- 在视频画面上绘制绿色骨架线
- 连接手指关节点
- 与YOLO检测框同时显示

**阶段3验收标准**:
- [ ] 能检测手部21个关键点
- [ ] 实时显示绿色骨架线
- [ ] 性能不影响实时性

---

## 四、文件修改清单

### 阶段1修改文件

| 文件 | 修改内容 |
|------|---------|
| `StepConditionEvaluator.cs` | 修复区域定义硬编码 |
| `SOPStateMachine.cs` | 修复对象跟踪算法 |
| `SOPModuleView.xaml` | 完善结果展示UI |
| `SOPModuleView.xaml.cs` | 添加结果更新逻辑 |

### 阶段2修改文件

| 文件 | 修改内容 |
|------|---------|
| `MainViewModel.cs` | 添加计时器逻辑 |
| `SOPModuleView.xaml` | 优化步骤卡片样式 |
| `SOPModuleView.xaml.cs` | 添加时间轴缩略图 |
| `DrawSOPDetectionResults` | 优化绘制效果 |

### 阶段3修改文件

| 文件 | 修改内容 |
|------|---------|
| 新增 MediaPipe 集成 | 手部姿态估计模块 |
| `MainViewModel.cs` | 集成姿态估计到检测流程 |
| `DrawSOPDetectionResults` | 添加骨架线绘制 |

---

## 五、测试计划

### 单元测试
- [ ] 区域定义读取测试
- [ ] IOU计算测试
- [ ] 对象跟踪稳定性测试

### 集成测试
- [ ] 完整8步SOP流程测试
- [ ] 跳步检测测试
- [ ] 违规检测测试

### 性能测试
- [ ] 实时性测试（目标>15FPS）
- [ ] 内存占用测试
- [ ] 长时间运行稳定性测试

---

## 六、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| MediaPipe集成复杂 | 阶段3延期 | 阶段1-2可独立交付 |
| 对象跟踪精度不足 | 误检/漏检 | 调优IOU阈值，添加置信度过滤 |
| 性能不达标 | 卡顿 | 降级检测频率，使用GPU加速 |

---

## 七、交付里程碑

| 里程碑 | 日期 | 交付物 |
|--------|------|--------|
| 阶段1完成 | T+3天 | 修复基础bug，系统可正确检测 |
| 阶段2完成 | T+7天 | UI优化完成，界面接近视频 |
| 阶段3完成 | T+14天 | 手部姿态估计集成（可选） |

---

**备注**: 
- 先完成阶段1，确保系统核心功能正确
- 阶段2提升用户体验
- 阶段3根据时间和需求决定是否实施
