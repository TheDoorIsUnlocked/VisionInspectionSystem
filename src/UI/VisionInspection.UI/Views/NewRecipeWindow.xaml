<Window x:Class="VisionInspection.UI.Views.NewRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Icon="/Assets/AppIcon.ico"
        Title="新建产品配方" Height="300" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🆕 新建产品配方" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="产品名称" FontSize="12" Foreground="#666" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox" Height="30" VerticalContentAlignment="Center"
                     TextChanged="NameBox_TextChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="基于模板" FontSize="12" Foreground="#666" Margin="0,0,0,4"/>
            <ComboBox x:Name="TemplateBox" Height="30" DisplayMemberPath="DisplayName"
                      SelectedValuePath="FullPath"/>
        </StackPanel>

        <TextBlock Grid.Row="3" TextWrapping="Wrap" Foreground="#999" FontSize="11" LineHeight="18">
            <Run Text="空白模板：生成最小可用骨架（步骤1 + 成品漏放校验步），区域与必放物料需自行在「🎯 标定区域」中定义。"/>
            <LineBreak/>
            <Run Text="基于现有配方：复制其全部区域/步骤/模型配置，仅替换产品名，便于同产线快速改型。新文件会立即出现在左侧产品配方下拉框中。"/>
        </TextBlock>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="OkButton" Content="确定" Width="80" Height="32" Margin="0,0,8,0"
                    Background="#1890FF" Foreground="White" IsEnabled="False"
                    Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="32"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
