# 2026-07-28 工作日志

## Ultralytics 制造业缺陷检测 & SOP合规案例调研

完成了针对 Ultralytics 官网、中文站/公众号及 WG Tech Solutions 的案例调研，生成报告文件：
`Ultralytics制造业缺陷检测与SOP合规案例调研报告.md`

### 关键发现

1. **Ultralytics 官网制造业方案**：
   - 5大场景：质量检测、PPE安全、预测性维护、装配验证、库存物流
   - 精度基准：电路板 99.1% mAP / 薄膜 714 FPS / 纺织品 89.4% mAP
   - 支持 6 种任务类型：检测、实例分割、语义分割、分类、姿态估计、OBB

2. **WG Tech Solutions SOP 合规案例（核心参考）**：
   - 平台：WGDeepInsight，使用 YOLO11 + YOLOv8
   - **45 个专用模型**而非 1 个通用模型（按工位/相机角度/物体大小分别训练）
   - Axelera Metis 硬件（M.2/PCIe/Compute Board 混合部署）
   - 架构：摄像头 → Voyager SDK 推理 → 规则引擎 → 仪表板/邮件/消息告警
   - SOP 场景：步骤顺序验证、托盘处理、PPE合规、未授权访问、过度拥挤、CCTV人员追踪、质量检查工作流
   - 成果：安全违规减少 28%

3. **对 SOP-AI 项目的启示**：
   - 可考虑为不同 SOP 步骤训练专用模型
   - WG Tech 的多通道告警机制（邮件/消息/角色仪表板）可参考
   - 质量检查工作流中"验证工具使用"和"任务时间监控"可作为扩展功能
   - Docker 容器化部署可简化跨产线迁移

## WG Tech SOP 合规检测技术深度解析（第二轮调研）

生成了第二份报告：`WG-Tech-SOP合规检测技术深度解析与代码参考.md`

### 新增技术细节

1. **Axelera 社区技术深度解析**：
   - WG Tech 团队（@kannan, @WGPravin, @hrithik_naik）分享了工程细节
   - 硬件策略：跨 Metis M.2/PCIe/Compute Board 部署，同一 Voyager 管线无需重写
   - 模型策略：45个专用模型按 PPE/安全区域/过程验证/缺件/缺陷/人员/安防 分类
   - YOLO11 vs YOLOv8：新用例用 YOLO11，已有验证管线保持 YOLOv8
   - 性能：单流 25-30 FPS，端到端告警延迟满足工厂监控需求
   - 还使用了 FaceNet 架构（定制运行在 Axelera 硬件上）

2. **Voyager SDK 代码资源**：
   - GitHub: https://github.com/axelera-ai-hub/voyager-sdk (v1.7.0)
   - Pipeline Builder API (Alpha)：op.seq/par/for_each 组合操作符
   - 支持 .axm (硬件推理) / .onnx (CPU推理) / .axe (可移植管线包)
   - 级联管线：/ax_models/reference/cascade 和 with_tracker 目录
   - 跟踪器：ByteTrack/OC-SORT/SORT/TrackTrack

3. **Ultralytics Axelera 导出完整代码**：
   - 导出命令：`yolo export model=yolo11n.pt format=axelera`
   - Python API: `model.export(format="axelera", quantize=8, data="cal.yaml")`
   - 性能基准：YOLOv8n PCIe 847 FPS / M.2 771 FPS；YOLO11n PCIe 746 FPS
   - 校准建议：100-400 张图像

4. **完整推理示例代码（GitHub 官方）**：
   - yolo26-pose-tracker.py：姿态估计+跟踪完整源码
   - yolo11-seg.py：实例分割完整源码
   - 自定义操作符模式：ConfidenceFilter(op.Operator)
   - 多对象跟踪：op.tracker(algo="tracktrack") 一行集成

5. **WG Tech 官网质量检测方案**：
   - 3类缺陷：表面/外观、缺件、尺寸/螺纹
   - 硬件产品线：Premium Plus (PCIe) / Premium (M.2) / Standard (Metis)
   - 企业级套件型号：DII7AP / DIA5AM2 / DIR3AM2

## 缺陷检测专项案例与部署指南（第三轮调研）

生成了第三份报告：`缺陷检测专项案例与部署指南.md`

### 内容概要

1. **4 个完整案例（含全部代码）**：
   - PCB 电路板缺陷检测：6 类缺陷，mAP@50 96.2%，60 PCBs/分钟，完整训练→评估→部署管线
   - 电子元件外壳缺陷检测：划痕/缺料/气泡，含 PLC 串口分拣联动代码
   - 汽车零部件车间安全行为检测：安全帽检测 + 危险区域入侵，违规降 82%
   - YOLO12 工业质检系统：含 TensorRT C++ 部署、多尺度推理、IndustrialDefectDetector 类

2. **部署资源清单**：
   - 硬件配置参考表（GPU 服务器/边缘设备/CPU/MCU/加速器）
   - 软件依赖完整安装命令
   - 公开数据集资源（DeepPCB/Roboflow/Kaggle）

3. **19 种导出格式速查表**：
   - 按部署目标推荐格式（NVIDIA/Intel/Apple/Jetson/华为/移动端等）
   - 完整格式列表和参数
   - 量化精度支持矩阵

4. **生产环境最佳实践**：
   - 模型热切换（不停机更新）代码
   - 异常恢复机制（CUDA 重置）
   - 温度控制策略
   - Docker 容器化部署方案
   - 完整部署 Checklist

5. **性能调优**：
   - FP32/FP16/INT8 量化性能对比数据
   - 性能瓶颈排查（预处理 35%/NMS 28%/内存拷贝 22%）
   - 阈值调优建议表
   - 常见问题解决方案

## 案例二可视化补充（第四轮）

为 `缺陷检测专项案例与部署指南.md` 中的案例二补充了可视化内容：

1. **SVG 系统架构图**：
   - 替代了原先报告中的纯文本 ASCII 架构图
   - 完整展示：工业相机 → ROI 裁剪 → YOLOv11 推理 → 缺陷分类 → 正常通过 / PLC 分拣 / 缺陷日志
   - 标注关键参数：2000 万像素 · 5FPS / conf=0.7 / imgsz=640 / COM3 · 9600 baud
   - 已作为内联可视化组件展示

2. **AI 生成缺陷样例图**（用于数据标注参考和方案演示）：
   - `image/defect_examples/defect_scratch.png` — 划痕缺陷（scratch 0.95）
   - `image/defect_examples/defect_missing_material.png` — 缺料缺陷（missing_material 0.92）
   - `image/defect_examples/defect_bubble.png` — 气泡缺陷（bubble 0.88）
   - 已在报告 3.4 节嵌入，并注明需用真实产线图像训练

## DWPose 手部姿态接入 SOP 系统（第五轮 — 用户要求"两者都执行"）

用户纠正：网图 `0_IMG_0631.png` 不是本项目截图，且 MediaPipe Hands 手感不丝滑。判断该图用的是
**DWPose / RTMPose（全身/wholebody 姿态估计）**。用户要求：(a) 写 DWPose 21 点手部接入代码；(b) 画 SOP 检测系统架构图。两项均已交付。

### 两项交付物

1. **DWPose 代码集成（C#，已编译验证）**
   - `HandPoseEstimationModels.cs`：新增枚举 `HandDetectionBackend { Auto=0, MediaPipe=1, Yolo=2, DWPose=3 }`；
     `HandPoseEstimationConfig` 增加 `Backend` / `DWPoseDetModelPath` / `DWPosePoseModelPath` / `DWPoseModelDir`
     （默认 `@"e:\yolo\YoloDotNet-master\DWPose-onnx\models"`）。
   - `IHandPoseEstimationService.cs`：重写 `InitializeAsync` 模型路径解析——优先用显式 `DWPoseDetModelPath/PoseModelPath`，
     否则回退到 `DWPoseModelDir`（或 `PalmModelPath` 作目录）+ 默认文件名 `yolox_l.onnx` / `dw-ll_ucoco_384.onnx`。
   - `SOPModule.cs`：新增字段 `_handBackend`；从 `sop_config.json` 的 `HandDetectionBackend` 读取；重写后端选择逻辑，
     支持 DWPose/MediaPipe/Yolo 强制分支 + Auto 兜底（Auto 保留原 MediaPipe→YOLO→DWPose 顺序）。
   - `src/UI/VisionInspection.UI/configs/sop_config.json`：新增 `"HandDetectionBackend": "DWPose"`。
   - 已存在未改动：`Services/DWPoseHandDetector.cs`（约 1500 行，实现 YOLOX det + DWPose pose 两阶段、SIMCC 解码、Kalman 跟踪）。
   - `HandSkeletonConnections`（21 点）后端无关，DWPose 输出直接映射，绘制层无需改动。

2. **SOP 检测系统架构图（内联 SVG）**：双管线 —— 摄像头/视频流 → (YOLO 物体检测 ‖ DWPose 手部姿态) →
   步骤规则引擎（SOPStateMachine + Workflow）→ (实时告警 | 可视化仪表板 | defect_log)。

### 辅助文件

- `scripts/dwpose_hand_inference.py`（NEW）：用与 C# 相同的 ONNX 模型做 Python 快速验证，复现两阶段逻辑，
  `decode_yolox` / `decode_simcc`(split=2.0) / `extract_hand`(kp 91/113, 每手 ≥3 有效点) / 支持摄像头/图片/视频 + `--cpu`。
- `docs/DWPose接入SOP系统指南.md`（NEW）：为什么选 DWPose、模型文件清单、改动清单、启用方式、Python 验证、调优、故障排查。

### 关键结论 / 注意事项

- **根因**：SOPModule 原硬编码 MediaPipe→YOLO→DWPose 优先级，而 `yolo11n-pose-hands.onnx` 始终存在，
  导致 DWPose 作为兜底永远不可达。新增显式 `Backend` 配置项后可强制启用，无需改代码。
- **DWPose ONNX 模型已就位**：`DWPose-onnx/models/yolox_l.onnx`(216MB) + `dw-ll_ucoco_384.onnx`(134MB)。
- **编译验证**：`dotnet build` 0 错误 / 33 警告（均为既有 nullability/obsolete 警告，非本次改动）。
- **潜在 bug（已记录建议，未改代码）**：C# `DWPoseHandDetector.PreprocessForDetection` 当前未做 /255 归一化
  （Python 参考用标准 /255）。可能是 YOLOX 人体召回偏低的原因，后续可修。
- **MediaPipe 现状**：palm 模型已改名 `.bak`，MediaPipe 分支会被跳过。
